# frontend/nixpacks.toml

[phases.install]
cmds = ["cd /app/frontend && bun install --frozen-lockfile"]

[phases.build]
cmds = ["cd /app/frontend && bun run build"]

# Static site - no start command needed
[start]
cmd = "echo 'Static site - files served by Coolify'"
