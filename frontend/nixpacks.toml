# frontend/nixpacks.toml

[phases.install]
cmds = ["cd /frontend && bun install --frozen-lockfile"]

[phases.build]
cmds = ["cd /frontend && bun run build"]

# Static site - no start command needed
[start]
cmd = "echo 'Static site - files served by Coolify'"
