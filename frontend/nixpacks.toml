# frontend/nixpacks.toml

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run build"]

# Static site - no start command needed
# The dist folder will be created in the current working directory
[start]
cmd = "echo 'Static site - files served by Coolify'"
